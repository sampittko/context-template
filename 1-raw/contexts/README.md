# Imported Contexts

Use `1-raw/contexts/<name>/` for other context repos or exported context slices that inform the current context.

Treat these as source inputs to the current context, not as active nested workspaces of this repo.

If a child context needs independent authoring and commits, keep it as its own repo and ingest only the approved surfaces here.
