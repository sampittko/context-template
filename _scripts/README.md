# Local Scripts

`_scripts/` stores optional tooling for this context.

Use it for wiki compilers, source refresh helpers, or migration utilities that support the repo without becoming part of the semantic knowledge layer.

Keep authored knowledge in `2-wiki/` and executable tooling in `_scripts/`.
