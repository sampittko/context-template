# Nested Contexts

Use `contexts/<name>/` for narrower contexts inside this context repo.

By default, the repo `.gitignore` ignores nested context repos while keeping this file tracked.

If a nested context is created, work inside that context should be committed in that nested repo, not in the parent repo.
